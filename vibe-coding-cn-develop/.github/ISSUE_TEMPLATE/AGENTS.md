# .github/ISSUE_TEMPLATE/ Agent 指南

本目录维护 GitHub Issue 模板。

## 约束

- 模板字段必须服务于复现、定位和归类。
- 字段保持简洁，优先收集问题、期望结果、复现路径和相关链接。
- 不加入无意义的长免责声明。
- 不要求用户公开隐私信息、密钥、Token 或账号凭证。
- 模板变更后运行 `make lint` 或 `make test` 检查 Markdown 格式。
